user - { collectionName: "string" };

addToCollection - { collectionName: "string", to: "string", from: "string" };

createCollection - { collectionName: "string" };

// Send name with signup / login
// Send token with both login and singup

addToHome - {};
